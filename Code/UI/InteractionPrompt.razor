@using CardGame.Characters

@inherits PanelComponent
@implements Component.ITriggerListener
@namespace CardGame.UI

<root>
	@if ( CanInteract && PlayerCharacter.Local.CanInteract )
	{
		<InputGlyph action="@InteractKey"></InputGlyph>
	}
</root>
