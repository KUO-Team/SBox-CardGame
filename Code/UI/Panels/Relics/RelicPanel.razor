@using Sandbox.UI

@inherits Panel
@namespace CardGame.UI

@if ( Relic is null )
{
	return;
}

<root>
	@if ( !string.IsNullOrEmpty( Relic.Icon ) )
	{
		<img class="relic-icon" src="@Relic.Icon" alt="Relic Icon"/>
	}
</root>
